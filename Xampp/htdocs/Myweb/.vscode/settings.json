{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "DuckDB",
            "name": "moto",
            "database": "${workspaceFolder:Myweb}/data.sql"
        }
    ]
}